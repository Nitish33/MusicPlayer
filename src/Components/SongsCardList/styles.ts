import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  songNameStyle: {
    textAlign: 'center',
    color: 'white',
    fontSize: 30,
    marginTop: 40,
    fontWeight: 'bold',
  },

  artistNameStyle: {
    textAlign: 'center',
    fontSize: 15,
    color: 'white',
    marginTop: 5,
  },

  listStyle: {
    width: '100%',
    overflow: 'visible',
    zIndex: 100,
  },
});

export default styles;
